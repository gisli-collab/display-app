{
  "name": "store-display-app-csv-manual",
  "version": "1.2.0",
  "private": true,
  "description": "Static Netlify product display app using manual CSV upload/replacement with barcode lookup and local price editing.",
  "scripts": {
    "start": "npx serve .",
    "build": "echo 'Static site; no build step required'"
  }
}
