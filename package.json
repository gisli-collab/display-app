{
  "name": "store-display-app-barcode-price",
  "version": "1.1.0",
  "private": true,
  "description": "Netlify display app that loads product data from n8n, scans barcodes, and sends price updates to n8n.",
  "scripts": {
    "start": "netlify dev",
    "build": "echo 'Static site; no build step required'"
  }
}
