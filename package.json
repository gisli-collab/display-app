{
  "name": "display-app-v1.6",
  "version": "1.6.0",
  "private": true,
  "description": "Static Netlify product display app with manual CSV upload, barcode lookup, local price, shelfcode, and status editing.",
  "scripts": {
    "start": "npx serve .",
    "build": "echo 'Static site; no build step required'"
  }
}
